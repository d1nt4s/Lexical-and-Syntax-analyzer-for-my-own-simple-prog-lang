// Примеры с семантическими ошибками (синтаксически правильные)
// Эти примеры должны парситься успешно, но при семантическом анализе
// должны выдавать ошибки с правильными позициями благодаря spans

// 1. Дублирование полей в struct
struct Test {
    int x;
    real y;
    int x;  // ОШИБКА: дублирование поля 'x'
}

// 2. Дублирование членов в enum
enum Direction {
    North,
    South,
    East,
    North  // ОШИБКА: дублирование 'North'
}

// 3. Использование несуществующей переменной
int a = 10;
int b = c;  // ОШИБКА: переменная 'c' не объявлена

// 4. Использование несуществующего поля
struct Point {
    int x;
    int y;
}
struct Point p;
p.z = 10;  // ОШИБКА: поле 'z' не существует в struct Point

// 5. Неправильный тип в присваивании
int num = 5;
real pi = 3.14;
bool flag = true;
num = pi;  // ОШИБКА: попытка присвоить real в int (если проверяется)

// 6. Вызов несуществующей функции
func int test() {
    return 1;
}
int result = unknownFunc();  // ОШИБКА: функция 'unknownFunc' не объявлена

